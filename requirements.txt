# FreshMart Data Pipeline — Module 3
# Install with: pip install -r requirements.txt

pandas==3.0.2
numpy==2.4.4
pyarrow>=17.0.0
great_expectations==1.20.0
pytest==9.1.1

# Airflow is the orchestration layer.
# Install it in a dedicated Airflow/Linux/WSL environment
# using the official Airflow constraints file.
apache-airflow==2.10.5
